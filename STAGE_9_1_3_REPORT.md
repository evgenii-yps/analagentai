# Этап 9.1.3 — замер подвижного выхода: пересчёт 8.10 и теневой прогон на позициях

Дата: 31.08.2026. Ветка `claude/trailing-shadow-measurement-9-1-3` от `main` (`a3c3e34`).

**Этап замерный. Ни одно правило системы не изменено.** `LOGIC_VERSION` остался 5,
позиции остались виртуальными. Не тронуты `src/agents/*`, `src/decision/*`,
`src/notify/*`, `src/evaluator/*`, `src/risk/*`, `src/barrier/*`,
`src/trailing/*`, `src/positions/rules.py`, `src/positions/runner.py`,
`src/export/*`, пороги, веса, состав агентов, `BARRIER_STOP_PCT`, `POSITION_*`.

**Параметр для внедрения не выбран и не предложен.** Ни в коде, ни в выводе
скриптов, ни в этом отчёте нет рекомендации «внедрить A=…, R=…».

---

## 1. Фактические имена объектов Этапа 8.10 (§2.1, критерий приёмки 10)

В проекте они не были записаны ни на одной странице. Взяты из кода:

| что | фактическое имя |
|---|---|
| правило подвижного выхода | `src/trailing/rule.py`, функция **`resolve_all`** |
| расчёт и запись | `src/trailing/runner.py`, функции `compute`, `compute_pair`, `build_rows`, `run` |
| точка входа | `src/trailing_main.py` → **`python -m src.trailing_main`** |
| таблица результатов | **`trailing_outcomes`** (миграция `017_trailing_outcomes.sql`) |
| проверка контроля | **`DB.check_trailing_control`** (`src/core/db.py`), вызывается из `runner.compute` |
| остальные методы БД | `get_trailing_anchors`, `get_trailing_pairs_done`, `save_trailing_outcomes`, `delete_trailing_outcomes`, `ensure_trailing_schema` |
| сетка вариантов | `ACTIVATION_RATIOS`, `RETRACE_RATIOS`, `TRAILING_VARIANTS`, `VARIANTS`, `FIXED_VARIANT` |
| причина выхода | **`EXIT_TRAIL = "trail"`** (не `trailing` — см. §6) |
| ночная задача | `deploy/agent-trade-trailing.cron`, **04:40 UTC** |
| три защиты от подгонки | `scripts/trailing_stats.py`: `spread_permutation`, `bootstrap_diff`, `best_variant` |

## 2. Что сделано

### Часть А — `scripts/trailing_resample_9_1_3.py`, только чтение

Расчёт 8.10 **не изменён ни одной строкой** и запускается как есть. Скрипт
читает уже посчитанное и печатает четыре блока: состав выборки; три таблицы 4×3
(вся выборка / старая часть / новая часть); три защиты от подгонки; проверку на
независимой половине с **двумя** ответами.

Три защиты **берутся из `scripts/trailing_stats.py` вызовом**, а не переписаны:
`spread_permutation` (10 000 перестановок), `bootstrap_diff` (10 000 пересборок,
95%), `best_variant`. Формулировки вердиктов импортируются оттуда же дословно.
Сетка 4×3 импортируется из `src/trailing/rule` и в скрипте не переобъявляется.

### Часть Б — `src/shadow/trailing.py` (чистый) + `scripts/shadow_trailing_9_1_3.py`

Своего определения A и R нет. Двенадцать подвижных вариантов считаются вызовом
**`src.trailing.rule.resolve_all`**, контроль — вызовом
**`src.positions.rules.check_exit`**, той самой функции, которой закрыты
настоящие позиции.

**Выноса функции из `src/trailing` не потребовалось** (§3.2 допускал его):
`resolve_all` уже принимает ряд баров параметром и уже чистая.

Согласовать пришлось только границы окна — и они **не подгоняются, а
сверяются**. `resolve_all` строит окно от бара решения; у позиции окно идёт от
бара после входа до бара срока. Эквивалент момента решения — `opened_at` минус
один бар; совпадение проверяется в `check_window`, расхождение роняет расчёт.

`armed` считается отдельным проходом (правило его не возвращает, а из причины
выхода он не выводится: позиция может включить механизм и досидеть до срока).
Уровень включения берётся всё той же `rule.activation_price`. Согласованность
двух проходов не декларируется, а проверяется двумя инвариантами: выход `trail`
невозможен без задетости, выход `stop` невозможен при задетости.

Издержки: `cost_pct` уходит параметром в оба правила, каждое вычитает его своим
штатным способом. Своей арифметики издержек в модуле нет. `net_pnl_usd`
считается от `positions.notional_usd` фактической позиции.

Годность бара — `settle_seconds()` **импортом** из `src/barrier/runner.py`,
формула не скопирована (проверяется тестом по тексту скрипта).

### Миграция 021

`db/migrations/021_position_trailing_shadow.sql` + откат. `positions` не
изменяется ни одной колонкой; внешний ключ смотрит из новой таблицы наружу.

## 3. Что проверено НА НАСТОЯЩЕЙ БАЗЕ

В контейнере оказался PostgreSQL 16 — поэтому критерий приёмки 1 проверен
исполнением, а не рассуждением. Поднят временный кластер, применены `db/init.sql`
и все миграции 007–021 по порядку.

| проверка | результат |
|---|---|
| миграция 021 применяется | ✅ таблица, индекс и **9 ограничений** созданы |
| `positions` на этой базе | ✅ **37 колонок** — ровно список §11.3 |
| повторное применение 021 | ✅ идемпотентно, строки и ограничения не тронуты |
| `ON DELETE CASCADE` | ✅ удаление позиции уносит теневые строки |
| откат | ✅ таблица удалена, `positions` осталась с 37 колонками |
| повторный откат и применение заново | ✅ без ошибок |

**Ограждения таблицы проверены вставками, а не чтением файла:**

| вставка | результат |
|---|---|
| `armed=true`, `armed_at` пуст | ✅ отвергнуто (`..._armed_chk`) — §7.11 |
| `armed=true`, `armed_at` есть | ✅ принято |
| `exit_reason='trailing'` (слово из §4 ТЗ) | ✅ **отвергнуто** — прямое доказательство ошибки ТЗ №1 |
| подвижный `ambiguous` без цены и итога | ✅ **принято** — доказательство, что `NOT NULL` был невозможен (ошибка ТЗ №3) |
| подвижный `target` | ✅ отвергнуто (`..._reason_variant_chk`) |
| `control` с `ambiguous` **и** ценой | ✅ принято |
| `control` с `trail` | ✅ отвергнуто |

**Оба скрипта прогнаны против настоящей базы.** Часть Б на закрытой позиции
(вход 100, цель +2%, предел −1%, слот $2, минутные свечи): контроль совпал
(`Разошлось: 0`), записано 13 строк, повторный прогон не изменил ни числа строк,
ни значений. Арифметика сходится точно: при вершине +2.5% и R=0.33 записано
`(1−0.33)×2.5−0.22 = 1.455`. Часть А на 120 синтетических парах напечатала все
четыре блока, обе таблицы половин и оба ответа проверки.

Отдельно: когда свечей в базе не оказалось, часть Б **отказалась** с кодом 2 и
текстом «живое правило не дало исхода на прочитанном ряде свечей» — блокирующее
условие §3.3 сработало на настоящих данных, а не только на двойнике.

## 4. Контрольные опыты (§11.2) — обязательная часть, а не украшение

Зелёный результат сам по себе не отличим от отсутствия проверки. Поэтому в
каждый дефект возвращались по очереди. **Два опыта нашли настоящие дыры в
проверках, и обе закрыты.**

| # | возвращённый дефект | до правки | после |
|---|---|---|---|
| 1 | окно сдвинуто на бар (`decision_ts_for_position` без сдвига) | **16 падений** | — |
| 2 | допуск контроля по цене расширен (8 → 2 знака) | **не ловилось** ❗ | 1 падение |
| 3 | допуск контроля по итогу расширен (6 → 4 знака) | **не ловилось** ❗ | 1 падение |
| 4 | `data_gap` перестал исключаться в SQL | **не ловилось** ❗ | 1 падение |
| 5 | отбор перестал требовать `status='closed'` | не ловилось ❗ | 1 падение |
| 6 | убрано соединение с `instruments` (дефект 9.1.2.2) | **10 падений** | — |
| 7 | момент сигнала переименован (`ts` → `signal_ts`) | **не ловилось** ❗ | 3 падения |
| 8 | убран второй ответ проверки на половине | 2 падения | — |
| 9 | токен взят из другой колонки (`base` → `symbol`) | — | 3 падения |

Что было не так и как исправлено:

* **Опыты 2–3.** Испорченная позиция отличалась слишком грубо (101.00 вместо
  102.00), и такое расхождение переживает любое округление. Добавлена проверка
  `test_a_difference_in_the_last_stored_digit_still_stops_the_run`: порча на
  ПОСЛЕДНЕМ знаке хранения — восьмом для цены, шестом для итога.
* **Опыты 4–5.** Двойник базы фильтровал `data_gap` **сам**, за базу. Двойник
  переписан так, что применяет условия, которые **реально стоят в запросе**.
* **Опыт 7.** Двойник отдавал ключи независимо от `SELECT`-списка. Добавлены
  `output_columns` и `project` в `tests/schema_double.py`: ключи ответа строятся
  из запроса, как в настоящей базе.

## 5. Дефект, который нашёл только реальный прогон

`get_trailing_resample_rows` отдавал момент сигнала как `signal_ts`, а
переиспользуемая `scripts.trailing_stats.collect` читает `ts` — часть А падала с
`KeyError` на первой же строке расчёта. **Ни одна проверка его не запускала**:
тестов на часть А не было вовсе.

Исправлено двояко: запрос отдаёт имя, которого ждёт переиспользуемая функция, и
добавлены четыре проверки, прогоняющие часть А целиком. Опыт 7 показывает, что
теперь этот дефект ловится.

Это тот же урок, что дважды был на 9.1.2.2: **проверка, которая ничего не
запускает, ничего и не доказывает.**

## 6. Ошибки в ТЗ — названы, а не обойдены

**1. Причина выхода называется `trail`, а не `trailing`** (§4). Схема ТЗ требует
`CHECK (exit_reason IN (…,'trailing'))`, но `src/trailing/rule.py` пишет
`EXIT_TRAIL = "trail"` с Этапа 8.10, и так же лежит в `trailing_outcomes`.
Принять `'trailing'` значило бы завести второй словарь для одного механизма и
переводить между ними при каждой записи. В миграции стоит `'trail'`; проверено
вставкой — `'trailing'` отвергается.

**2. В перечне §4 нет `no_data`, а правило его возвращает.** Ряд свечей позиции
может оказаться неполным (`contiguous_prefix` обрезает окно по первому разрыву),
и тогда исход подвижного варианта не измерен. Добавлен в `CHECK`.

**3. `exit_bar_ts`, `exit_price`, `net_pnl_pct`, `net_pnl_usd` не могут быть
`NOT NULL`** (§4). У `ambiguous` и `no_data` подвижного варианта бара выхода не
существует. `NOT NULL` заставил бы подставить туда выдуманное число. Поля
сделаны nullable, нужная форма записана `position_trailing_shadow_shape_chk` —
тем же приёмом, что `trailing_outcomes_shape_chk` в миграции 017. У контроля поля
заполнены всегда, включая `ambiguous`: живое правило позиций при одновременном
касании выходит по пределу, с настоящей ценой.

**4. §7.2 требует у подвижных вариантов «исход target».** Такого исхода у них не
бывает и быть не может: уровень включения A ≤ 1 лежит не дальше цели, поэтому
цена, дошедшая до цели, уже прошла порог включения. Это документированное
свойство правила 8.10 (заголовок `src/trailing/rule.py`, пункт 2), записанное
ограничением `trailing_outcomes_reason_variant_chk`. Проверяемая половина
требования — «подвижный выход не срабатывает раньше цели» — верна и проверяется.

**5. §2.3 не говорит, по какому времени делить выборку на старую и новую.**
Выбран `computed_at`: вопрос в том, какие данные появились ПОСЛЕ находки, а пара
становится посчитанной не когда пришёл сигнал, а когда истёк её горизонт. Запись
8.10 идёт с `ON CONFLICT DO NOTHING`, поэтому `computed_at` повторным прогоном не
сдвигается. Разбивка по времени сигнала печатается рядом для сверки, а пустая
старая часть (признак принудительного пересчёта) названа вслух.

**6. §9 команда `docker compose --profile "*" build` — верна**, но в §9 указано
`docker compose exec -T postgres psql … < 021…sql` до пересборки, тогда как
скрипты живут в образе `backtest`; порядок в §9 это учитывает, замечаний нет.

## 6-бис. Правка после боевого прогона: контроль части Б не сошёлся

Боевой прогон 31.08.2026: `shadow_control_compared=11`,
`shadow_control_mismatched=12`, ни одной строки не записано. Отказ сработал
правильно — разобраны три причины.

### 1. Сравнивались разные величины: открытие бара против его закрытия

**Ошибка в ТЗ.** §3.3 требовал сверять `exit_bar_ts`, но колонки с таким именем
в `positions` нет — и §11.3 того же ТЗ приводит полный состав таблицы, где её
действительно нет. Требование противоречило само себе. Момент закрытия лежит в
`closed_at`.

**Гипотеза подтверждена чтением кода**, а не принята на веру.
`src/positions/runner.py:231`:

```python
closed_at = (now if by_gap else decision.exit_bar_ts + timedelta(seconds=60))
```

`check_exit` возвращает время **открытия** бара выхода; `runner` пишет в
`closed_at` время его **закрытия**. Отсюда ровно 60 секунд на всех одиннадцати
позициях, факт позже пересчёта.

Добавлена `closing_moment(exit_bar_ts, resolution)`; сверка идёт через неё.
**Допуск остался нулевым** — изменилось не «насколько близко», а «что с чем».
Длина бара берётся по разрешению, а не литералом 60: у `runner` литерал верен
(`positions_resolution_chk` допускает только `1m`), но второе место со своим
литералом разошлось бы с первым в день, когда позиции научатся жить на часовом
ряде. Что правило `runner` не изменилось, проверяется по тексту его исходника —
`test_the_closing_rule_still_matches_the_live_runner`.

В таблицу по-прежнему пишется **открытие** бара: у контроля и у подвижных
вариантов это одна и та же величина, и приведение нужно только для сверки.

### 2. Задетость считалась по всему окну, включая бары после выхода варианта

**Воспроизведение разошлось с гипотезой правки, и это существенно.** Правка
предполагала, что дело в одном баре: максимум дошёл до порога, минимум — до
предела. Прогон показал другое:

| сценарий | что возвращает правило |
|---|---|
| порог и предел **в одном баре** | `ambiguous` для затронутых уровней, `stop` для более высоких — утверждение не нарушается |
| предел на баре 1, порог задет **позже выхода** | `stop` + `armed=true` → **AssertionError** |

Настоящая причина — **область просмотра**: `armed_scan` шёл по всему окну и
засчитывал задетость по бару, случившемуся через полчаса **после** того, как
вариант вышел. Механизм такую сделку не трогал — её уже не было. Ошибка не
только роняла расчёт, но и **завышала главное число этапа** (ЧИСЛО 1 отвечает
ровно на вопрос «сколько сделок механизм задел»).

`armed_scan` получил границу `until` — бар выхода варианта включительно. Для
`timeout`, `no_data` и `ambiguous` граница не нужна: там просмотр всего отрезка
точен (у `ambiguous` первым порога достигает как раз тот бар, на котором вариант
и остановился — достигни его бар раньше, правило включило бы подвижный выход).

**Утверждение не убрано, а сужено до «между барами»** и названо точно: предел не
может сработать после включения, случившегося на предыдущем баре; внутри одного
бара совпадение разрешается **тем же способом, что у живого правила** — исходом
`ambiguous`, и выход `stop` до этого случая просто не доходит. Своего способа не
изобретено. После сужения области задетости утверждение стало точным в обе
стороны и потому оставлено строгим.

### 3. Счётчик печатал невозможное: сверено 11, разошлось 12

Позиция, расчёт которой упал с исключением, вычиталась из сверенных, но
засчитывалась в расхождения. Теперь `compared = len(positions)`: такая позиция
**сверена** (попытка была) и **разошлась** (совпадения не получено), и входит в
оба счётчика одинаково. Добавлена явная проверка `mismatched > compared`,
роняющая расчёт: увидев «11 и 12», человек начинает сомневаться во всём выводе,
и правильно делает.

### Проверено на настоящей базе

Поднят PostgreSQL 16, применены `init.sql` и миграции 007–021, заведены две
позиции: закрытая по цели и **воспроизводящая боевую №6** (предел задет сразу,
порог включения — позже выхода).

| проверка | результат |
|---|---|
| `shadow_control_mismatched` | ✅ **0 из 2**, без единой поблажки |
| позиция вида №6 | ✅ считается без исключения; `armed=false` у всех 12 вариантов |
| задетых среди закрытых по `stop` | ✅ **0** |
| повторный прогон `--apply` | ✅ 26 строк, значения не изменились |

### Контрольные опыты (§11.2)

| # | возвращённый дефект | результат |
|---|---|---|
| 10 | момент закрытия снова сравнивается с открытием бара | **9 падений** |
| 11 | задетость снова считается по всему окну | 1 падение — тем же `AssertionError`, что на боевой базе |
| 12 | счётчик снова вычитает упавшие из сверенных | 1 падение |

Отдельно исправлена **тестовая фикстура**: она сама несла `closed_at` как время
открытия бара — ту же путаницу, из-за которой прогон и разошёлся. Двойник,
повторяющий ошибку проверяемого кода, ничего не проверяет.

## 6-трижды. Правка после боевого прогона: часть А убита по памяти

Боевой прогон: `scripts/trailing_resample_9_1_3.py` печатал блок 1 и молча
обрывался, код возврата 137. Журнал ядра, два прогона подряд: `anon-rss`
811 760 КБ и 919 340 КБ при `mem_limit: 1g` у службы `backtest`. Часть Б
отработала полностью (контроль 12 из 12, разошлось 0, записано 156 строк) — её
эта правка не касается.

### Где именно уходила память — замерено, а не предположено

| что | сколько при 131 380 парах |
|---|---|
| 1 707 940 строк словарями | **~1 727 МБ** |
| 131 380 пар словарями (`collect`) | ~154 МБ |
| **матрица чисел `N × 13 float64`** | **13 МБ** |
| порция бутстрэпа | 30 МБ, с ростом выборки **не растёт** |
| копия при перестановке | 13 МБ, освобождается каждый повтор |

Вывод, определивший всю правку: **считаемые числа занимают тринадцать
мегабайт**. Остальное — Python-объекты строк, нужные ровно на время сборки
одной пары. Значит трогать статистику не потребовалось вовсе.

### Что изменено в способе счёта

1. **Чтение порциями по ключу-границе** (`DB.fetch_trailing_resample_batch`).
   13 000 строк за раз, граница — ключ пары `(ts, signal_id, horizon_h)`, а не
   `OFFSET`: со смещением сотая порция стоила бы как сто первых. Хвостовая пара
   порции откладывается до следующей — обрезанная пара выглядела бы неполной и
   была бы выброшена из сравнения по причине, которой в данных нет.
2. **Один проход, агрегаты вместо списков.** Состав выборки (блок 1) считается
   счётчиками попутно; пары складываются в растущую матрицу `float64` по
   104 байта на пару вместо словаря на ~1 200.
3. **Отбор пар — ТА ЖЕ функция.** `scripts.trailing_stats.collect` вызывается
   на тринадцати строках одной пары вместо всей выборки: он группирует по
   ключу, поэтому на одной паре отвечает «годится» либо «отброшена, вот
   почему». Второго экземпляра правила отбора не заведено.
4. **Порядок строк сохранён.** `ORDER BY s.ts, …` в запросе даёт ровно тот
   порядок, который получался после сортировки внутри `collect`. Это не
   косметика: от порядка зависят и деление выборки пополам, и последовательность
   случайных чисел в перестановочной проверке.

**Три защиты от подгонки не тронуты ни в одном месте.** 10 000 перестановок,
10 000 пересборок, те же зёрна, те же функции Этапа 8.10.

### Пиковое потребление: до и после

Замер отдельным процессом (`tests/memory/resample_memory_probe.py`), потому что
`ru_maxrss` не опускается и внутри общего прогона поймал бы чужой пик.

| способ | строк | пик RSS |
|---|---|---|
| **прежний** (вся выборка списком) | 1 707 940 | **1 204,8 МБ** — больше лимита 1 ГБ |
| **новый** (потоком) | 1 707 940 | **104,6 МБ** |
| новый, рост выборки втрое | 5 123 820 | **143,9 МБ** |

`mem_limit: 1g` **не поднят** — правка уложилась в прежний лимит с запасом
почти в десять раз, и запас на рост втрое тоже проверен. Отдельная проверка
следит за тем, чтобы лимит не подняли молча.

### Числа не изменились

`test_the_streaming_pass_gives_the_same_numbers_as_the_old_way` считает выборку
старым способом (всё разом, `collect` один раз) и новым (порциями по 47 строк —
границы намеренно рвут пары посередине) и сверяет матрицы `np.array_equal`, то
есть **побитово**, вместе с порядком строк.

Отдельно записан размен, от которого я **отказался**: одним вызовом
`bootstrap_means` на матрицу из 12 столбцов интервалы считаются в 13 раз быстрее
(1,2 минуты против 15,6 на 131 380 парах), но расходятся в **шестнадцатом
знаке** — умножение матрицы на матрицу и на вектор складывают числа в разном
порядке. На печать в четырёх знаках это не влияет никак, и всё же взят медленный
путь: правка была о памяти, и менять числа заодно с ней значило бы лишить
владельца возможности сверить новый вывод со старым. Проверка
`test_one_shared_bootstrap_would_differ_in_the_sixteenth_digit` фиксирует цену,
чтобы следующий увидел её сразу.

Общее время части А на боевом объёме: около 20 минут (15,6 бутстрэп + 3,8
перестановка). Это не регресс — столько же было и до правки.

### Тихий обрыв больше не тихий

* вывод начинается строкой «**ПРИЗНАК ЗАВЕРШЕНИЯ**: строка «РАСЧЁТ ЗАВЕРШЁН» в
  самом конце» и оканчивается этой строкой. Её отсутствие означает, что прогон
  не дошёл до конца, а не что считать было нечего;
* пиковая память печатается в конце и уходит в журнал ключом **`peak_rss_mb`**
  (проверено на живом прогоне: `peak_rss_mb=89.6 resample_pairs=200`);
* перед тяжёлой частью читается лимит контейнера (`cgroup v1/v2`), печатается
  оценка на текущий объём и на троекратный рост. Не помещается в бюджет
  (половина лимита) — **отказ с числами, код возврата 4**, и решение о лимите
  принимает владелец, а не ядро.

### Контрольные опыты (§11.2) — два нашли настоящие дыры

| # | возвращённый дефект | результат |
|---|---|---|
| 13 | прежний способ против потолка памяти | 1 204,8 МБ > 1 024 МБ — падает ✅ |
| 14 | потолок задран выше прежнего способа | 1 падение (проверка теряет зубы — видно) |
| 15 | убран признак завершения | 1 падение |
| 16 | отказ по памяти отключён | 1 падение |
| 17 | `LIMIT` убран из запроса | **не ловилось** ❗ → 1 падение |
| 18 | хвостовая пара порции не откладывается | 2 падения |
| 19 | ключ-граница убрана из запроса | **ВЕЧНЫЙ ЦИКЛ** ❗ → 1 падение |

Две находки, обе исправлены:

* **Опыт 17.** Двойник пула применял ключ-границу и предел порции **сам**,
  независимо от текста SQL. Та же болезнь «двойник мягче базы», что и на
  9.1.2.2. Двойник переписан: он делает ровно то, что написано в запросе.
* **Опыт 19.** Без ключа-границы каждая порция возвращала одно и то же начало
  таблицы, и `stream_sample` крутился **вечно**, печатая ноль строк и выглядя
  работающим. Снаружи это неотличимо от долгого счёта — тот же класс дефекта,
  что и тихий обрыв, ради которого правка затевалась. Добавлено ограждение:
  граница обязана двигаться вперёд, иначе расчёт падает и называет причину.

### Неточность в задании правки — названа, а не обойдена

§1 правки требует, чтобы «потребление памяти не росло линейно с числом строк», и
предлагает свести всё к суммам и счётчикам. Для блока 2 этого действительно
хватает, **но для блоков 3 и 4 — нет**: перестановочная проверка, парный
бутстрэп и деление выборки пополам работают с величинами КАЖДОЙ пары
одновременно, и по суммам их посчитать нельзя. Свести к агрегатам буквально —
значит упростить три защиты, что §2 той же правки запрещает прямо.

Поэтому линейность осталась, но с другим множителем: 104 байта на пару вместо
~13 200 байт строк той же пары — в 127 раз меньше. Числовой ориентир правки при
этом выполнен с большим запасом: 104,6 МБ на 1 707 940 строках и 143,9 МБ при
росте втрое против ориентира «уложиться в 1 ГБ».

## 7. Числа

* `tests/test_stage_9_1_3.py` — **43 проверки, все проходят** (27 → 33 → 43 по правкам);
* весь набор — **753 прошли, 75 пропущены** (пропуски прежние, требуют БД/сети);
* `ruff check .` — **All checks passed**;
* отпечаток решений: `scripts/decision_parity_8_7.py` → 323 случая,
  `1f12d5d29d64eb17911b2a20196311fdde6a66e9f932120a7d5d412aac0de18d` — **тот
  самый `1f12d5d2…de18d`**, решения не изменились (критерий 6);
* `mypy` из критериев исключён §11.5; проверялся только `ruff`.

Записи вне `position_trailing_shadow` отсутствуют: этап добавил в SQL ровно пять
операторов — четыре читающих и один `INSERT` в свою таблицу (критерий 8).

## 8. Хэш main (§11.1)

Снят запросом к удалённому репозиторию, а не по локальному логу:

```
$ git ls-remote origin main
c30f18fa9edd02c9b3e1b0971f32989fb2d12628	refs/heads/main
```

Слияние `--no-ff` дало **`c30f18f`** (было `a3c3e34`). Следом идёт коммит,
вносящий этот самый хэш в отчёт, — он сдвигает `main` ещё на шаг, до
**`5364bb9`**. Это не расхождение, а неизбежное следствие того, что отчёт
называет хэш ветки, в которую сам же и попадает: назвать одним числом и итог
слияния, и голову после записи отчёта нельзя. Обе величины названы здесь прямо,
и обе снимаются `git ls-remote origin main`.

Ветка
`claude/trailing-shadow-measurement-9-1-3` (`4e0d595`) на месте, не удалена.

**Правка §6-трижды (память) влита отдельно:** слияние `--no-ff` дало
**`244ff29`**. Хэш снят `git ls-remote origin main`. Слитое дерево проверено ДО
пуша: 753 проверки прошли, `ruff` чист, отпечаток решений тот же.

**Правка §6-бис влита отдельно:** слияние `--no-ff` дало **`cecd659`**
(было `4f70877`). Хэш снят `git ls-remote origin main`, а не по локальному логу.
Слитое дерево проверено ДО пуша: 743 проверки прошли, `ruff` чист, отпечаток
решений тот же.
Слитое дерево проверено ДО пуша: 737 проверок прошли, `ruff` чист, отпечаток
решений тот же.

## 9. Что осталось непроверенным без доступа к боевой машине

* **Главные числа этапа.** Обе части прогнаны только на придуманных и
  синтетических данных. Сколько позиций механизм задел на самом деле, устояла ли
  закономерность 8.10 на выросшей выборке — ответ даст первый прогон на сервере.
  Ни одного вывода о подвижном выходе этот отчёт не содержит и содержать не
  может.
* **Совпадение контроля части Б на боевых позициях.** Проверено, что механизм
  сверки работает и блокирует; совпадёт ли он на настоящих свечах — покажет
  прогон. Расхождение здесь ожидаемо и ценно: оно означало бы, что ряд свечей в
  базе изменился с момента закрытия позиции.
* **`control_mismatched = 0` части А** (критерий 2): `check_trailing_control` не
  вызывался — он требует заполненных `signal_outcomes_barrier` и
  `trailing_outcomes`. Скрипт части А эту проверку не дублирует намеренно: она
  живёт в штатном прогоне 8.10 и запускается им.
* **`deploy/verify_9_1.sh`** (критерий 7) и сравнение потока сигналов до/после —
  требуют боевой машины.
* **Время расчёта части Б** на реальном объёме. На одной позиции — доли секунды;
  чтение ряда идёт по одной позиции за раз, и на десятке позиций это десяток
  запросов.

## 10. Порядок развёртывания

Как в §9 ТЗ. После пересборки — часть Б вхолостую, и только при коде возврата 0
с `--apply`; затем часть А. Скрипты запускаются через образ `backtest`
(`docker compose --profile tools run --rm --no-deps backtest python scripts/…`),
отдельным cron не оформляются: это разовый замер.
