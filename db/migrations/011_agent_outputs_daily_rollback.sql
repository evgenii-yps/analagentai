-- ОТКАТ суточной свёртки выводов агентов (Этап 8.1).
--
-- ВНИМАНИЕ: таблица итогов удаляется безвозвратно. Пересчитать её можно только
-- из сырья, а сырьё живёт 90 суток — то есть после отката восстановятся итоги
-- не более чем за три последних месяца.
--
-- Перед откатом верните RETENTION_AGENT_OUTPUTS_DAYS=0 (не удалять журнал),
-- иначе выводы продолжат удаляться, а итогов уже не будет.

BEGIN;
DROP TABLE IF EXISTS agent_outputs_daily;
COMMIT;
