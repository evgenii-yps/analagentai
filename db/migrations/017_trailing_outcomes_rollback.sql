-- Откат миграции 017 (Этап 8.10 §6).
--
-- ВНИМАНИЕ: удаляет все посчитанные исходы подвижного выхода — тринадцать
-- строк на каждую пару (сигнал, горизонт). Потеря НЕ катастрофична: таблица
-- восстанавливается полным пересчётом (src/trailing_main.py) по
-- signal_outcomes_barrier и свечам. Но восстановление возможно ровно до тех
-- пор, пока живы минутные свечи: политика хранения удаляет ohlcv с
-- timeframe='1m' старше RETENTION_1M_DAYS, и после отката пересчитанные строки
-- получат resolution='1h' вместо '1m' — то есть станут ГРУБЕЕ прежних, а на
-- часовых барах подвижный выход измеряется заведомо хуже: вершина внутри
-- часового бара неизвестна.
--
-- Перед откатом на работающей системе снимите копию:
--   docker compose exec -T postgres pg_dump -U agenttrade -d agenttrade \
--       -t trailing_outcomes > /tmp/trailing_outcomes_backup.sql
--
-- Таблицы продакшна откат не затрагивает: внешний ключ смотрит из удаляемой
-- таблицы наружу, а не наоборот. Ни одно решение системы от неё не зависело,
-- поэтому после отката система работает ровно так же, как работала.

BEGIN;

DROP TABLE IF EXISTS trailing_outcomes;

COMMIT;
