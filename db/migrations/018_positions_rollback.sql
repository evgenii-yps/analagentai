-- Откат миграции 018 (Этап 9.1 §5).
--
-- ВНИМАНИЕ: удаляет ВСЕ виртуальные позиции — и открытые, и закрытые. Потеря
-- НЕВОССТАНОВИМА в строгом смысле: позиция ведётся по живым минутным свечам, и
-- пересчитать её задним числом можно только пока эти свечи ещё живы (политика
-- хранения удаляет ohlcv с timeframe='1m' старше RETENTION_1M_DAYS). Кода
-- пересчёта позиций задним числом в проекте нет и по замыслу быть не должно:
-- этап измеряет ведение позиции В ЖИВОМ ВРЕМЕНИ, включая задержку между
-- решением и входом, а задним числом эта задержка была бы выдумана.
--
-- Перед откатом на работающей системе снимите копию:
--   docker compose exec -T postgres pg_dump -U agenttrade -d agenttrade \
--       -t positions > /tmp/positions_backup.sql
--
-- Таблицы продакшна откат не затрагивает: внешние ключи смотрят из удаляемой
-- таблицы наружу, а не наоборот. Ни одно решение системы от неё не зависело,
-- поэтому после отката система работает ровно так же, как работала. Остановить
-- сервис positions до отката обязательно — иначе он пересоздаст таблицу своим
-- идемпотентным DDL при следующей итерации:
--   docker compose stop positions

BEGIN;

DROP TABLE IF EXISTS positions;

COMMIT;
