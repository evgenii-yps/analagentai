-- Откат миграции 023 (Этап 9.1.5 §5).
--
-- ПРОВЕРКИ НА НЕПУСТОТУ ЗДЕСЬ НЕТ НАМЕРЕННО, как и в откате 022. Таблица
-- signal_range_position содержит ТОЛЬКО производные величины: она целиком
-- восстанавливается повторным прогоном
-- scripts/range_position_9_1_5.py --apply по тем же самым signals и ohlcv.
-- Ни одного факта, которого нет больше нигде, в ней не лежит.
--
-- ВАЖНАЯ ОГОВОРКА О ВОССТАНОВИМОСТИ. Минутные свечи живут RETENTION_1M_DAYS
-- суток, поэтому восстановить строку по сигналу, чьё окно уже вышло за срок
-- хранения, будет НЕЛЬЗЯ. Это не делает откат опасным — производная величина
-- не факт, — но и объявлять восстановление безусловным было бы неправдой.
--
-- signals и ohlcv этот откат НЕ ТРОГАЕТ: внешний ключ смотрит из удаляемой
-- таблицы наружу, поэтому DROP не задевает ни одной строки факта.
--
-- Ручное применение:
--   docker compose exec -T postgres psql -U agenttrade -d agenttrade \
--       < db/migrations/023_signal_range_position_rollback.sql

BEGIN;

DROP TABLE IF EXISTS signal_range_position;

COMMIT;
