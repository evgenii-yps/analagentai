"""Исторический реплей ядра (Этап 7.4).

ГЛАВНЫЙ ПРИНЦИП: это РЕПЛЕЙ, а не новая модель. Прогон вызывает те же самые
чистые функции агентов и Decision Agent, что работают в продакшне
(``src.agents.market.analyze_ohlcv``, ``src.agents.futures.analyze_futures``,
``src.decision.agent.make_decision``), с теми же параметрами из
``src.core.config.settings``. Собственных реализаций индикаторов здесь нет —
если бы они были, измерялась бы другая система.

Пакет ничего не меняет в продакшне: пишет только в схему ``backtest``, читает
продакшн-таблицы исключительно на чтение, запускается отдельным контейнером
профиля ``backtest`` вручную.

ЭТАП 7.4 ЗАКРЫТ И БОЛЬШЕ НЕ ВЫПОЛНЯЕТСЯ (решение от 22.08.2026, ТЗ 8.1).
Код пакета сохранён намеренно — он документирует метод и может пригодиться
позже, — но прогоны не запускаются, а его результаты не поддерживаются.
"""
