# Этап 8.8 — вторая оценка исхода по границам

**Дата:** 26.08.2026 · **Исполнитель:** Claude Code
**Ветка:** `claude/stage-8-8-barrier-outcomes-dehs65` · **База:** `d61b6ce` (merge Этапа 8.7)

> **О среде рядом с каждым числом.** ТЗ §10 требует указывать, где получено число.
> В отчёте используются ровно две пометки:
> **[стенд]** — контейнер этой сессии: PostgreSQL 16.13, Python 3.12.3, код ветки;
> **[сервер]** — продакшн. **Ни одного числа с пометкой [сервер] в отчёте нет:**
> доступа к продакшн-базе у исполнителя не было. Что из этого следует и что обязан
> сделать владелец — в §2 и в разделе «Что НЕ сделано».

---

## §11. Дисциплина: сверка ветки перед первым коммитом

```
$ git rev-parse --abbrev-ref HEAD
claude/stage-8-8-barrier-outcomes-dehs65
```

Ветка совпадает с назначенной. Работа велась только на ней; форс-пуша не было.

**Расхождение, которое обязан знать владелец.** ТЗ называет базой `main (d61b6ce)`.
Фактически `origin/main` указывает на `0784251` и отстаёт на **38 коммитов** —
Этапы 8.1–8.7 в неё не влиты. Коммит `d61b6ce`, названный в ТЗ, лежит в истории
рабочей ветки, а не в `main`. Поэтому слияние в `main` **не выполнено**: оно
принесло бы в `main` не только Этап 8.8, но и всю невлитую историю 8.1–8.7, а это
отдельное решение владельца, а не следствие этой задачи. Ветка запушена;
команда слияния — в runbook.

---

## §1. Жёсткая граница: решения системы не изменились

| Утверждение | Как проверено | Результат |
|---|---|---|
| `LOGIC_VERSION` = 5, не поднят | `src/core/config.py:117`, тест | ✅ не тронут |
| `src/agents/`, `src/decision/`, `src/notify/` не изменены | `git diff --stat` | ✅ ни одного файла |
| `signals`, `signal_evaluations`, `signal_targets`, `risk_targets` не дополнены | миграция не содержит `ALTER` этих таблиц; раздел 3 `verify_8_8.sh` сверяет число колонок | ✅ |
| Текст уведомлений не изменён | `src/notify/wording.py` не тронут | ✅ |

**Отпечаток решений [стенд]:**

```
$ POSTGRES_PASSWORD=parity python scripts/decision_parity_8_7.py
# случаев: 323, отпечаток: 1f12d5d29d64eb17911b2a20196311fdde6a66e9f932120a7d5d412aac0de18d
```

Совпал с требуемым `1f12d5d2…de18d` на **323** случаях — до и после всех правок этапа.

Дополнительно **тестом**, а не обещанием: `test_barrier_is_not_imported_by_the_hot_path`
читает все файлы `src/agents/`, `src/decision/`, `src/notify/`, `src/evaluator/` и
падает, если в них появится ссылка на `src.barrier`. Тот же контроль — раздел 12
`verify_8_8.sh` на сервере.

---

## §2. Предпроверка — **ГЕЙТ НЕ ЗАКРЫТ**

ТЗ называет §2 блокирующей и требует чисел «ничего не предполагая». Честный ответ:
**чисел с продакшна в этом отчёте нет — доступа к серверной базе у исполнителя не было.**
Выдумывать их или выдавать стендовые за серверные нельзя — это ровно тот вид подделки,
против которого написан весь остальной ТЗ.

Поэтому предпроверка поставлена **исполняемым скриптом** `scripts/precheck_8_8.sql`
(только чтение, запускается на работающем продакшне не останавливая сервисы). Он
отвечает на все четыре вопроса §2 отдельными разделами:

| Вопрос §2 | Раздел скрипта | Что печатает |
|---|---|---|
| 1. Таймфреймы в `ohlcv`, глубина, пропуски | 1, 1b, 1c | глубина в сутках и возраст по каждому (инструмент × таймфрейм); число разрывов минутного ряда и самый длинный; **покрытие окон сигналов** по горизонтам: `covered` / `partial` / `none` |
| 2. Сигналы v5 с замороженными целями и с какого момента | 2 | `with_target` / `frozen_null` / `no_row` с `first_ts` и `last_ts` |
| 3. Сигналы v5 без целей | 3 | число и фактический интервал — сверяется с ожиданием ТЗ (22.08 22:59 — 24.08 20:42 UTC) |
| 4. Как хранится цель: направление, горизонт, единицы | 4 | состав колонок и ограничения `signal_targets` **из каталога БД**, плюс фактическая разбивка по (горизонт × направление) |

**Скрипт исполнен и его вывод проверен [стенд]** — на засеянных данных он печатает
осмысленные числа (например, раздел 1c: `horizon_h=4 → pairs 7, covered 3, partial 0, none 4`;
раздел 2: `with_target 6, no_row 1`). То есть работоспособность скрипта доказана,
а **значения продакшна остаются неизвестными**.

### На что ответ уже есть — из кода, а не из догадки

Вопрос 4 закрывается схемой, и она одинакова на стенде и на сервере (миграция 014
применена там и там). Прочитано из каталога БД **[стенд]**:

```
signal_targets: PRIMARY KEY (signal_id, horizon_h)
  direction        text            NOT NULL   ← направление хранится отдельной колонкой
  horizon_h        smallint        NOT NULL   ← и горизонт тоже
  price_at_signal  double precision NOT NULL
  target_pct       numeric(10,5)              ← ПРОЦЕНТЫ
  target_price     double precision           ← и цена, отдельной колонкой
```

**Ответ на §2.4: да, цель хранится отдельно по направлению и горизонту; единица —
процент (`target_pct`), цена продублирована в `target_price`.** Расчёт берёт
именно `target_pct`.

Вопрос 1 частично закрывается конфигурацией (проверяемо в репозитории):
`TIMEFRAMES=1m,5m,15m,1h` — минутные свечи **собираются**; `RETENTION_1M_DAYS=30` —
их глубина **ограничена 30 сутками сверху** политикой хранения, глубже их не будет
никогда. Сигналы версии 5 начались 22.08, то есть на момент сдачи минутный ряд
обязан их покрывать; факт покрытия печатает раздел 1c.

### Почему работа не остановлена

ТЗ: «Если минутных свечей нет или глубина меньше 30 суток — остановиться и доложить.
От этого зависит §4». Зависимость §4 от §2 **снята конструктивно**: разрешение
выбирается не один раз для всей системы, а **по каждой паре (сигнал, горизонт)
в момент расчёта**. Если минутный ряд покрывает окно целиком — считаем по нему и
пишем `resolution='1m'`; не покрывает — переходим на часовой и пишем `'1h'`, а
неразрешимые случаи честно получают `ambiguous`. Код верен при **обоих** исходах
предпроверки, поэтому её результат меняет не код, а **пригодность метрики**, и
именно это измеряется долей `ambiguous` (§4) и печатается отдельной строкой.

**Обязанность владельца:** первым шагом runbook выполнить `precheck_8_8.sql` на
сервере и приложить вывод. Если минутных свечей там не окажется — расчёт
отработает на часовых, и это будет видно числом, а не скрыто.

---

## §3–§5. Правило исхода: что именно реализовано

Чистый модуль `src/barrier/outcomes.py` (ни базы, ни сети, ни «сейчас» внутри).

Уровни от замороженной цены решения `P`; `target_pct` — **замороженный** из
`signal_targets`, `stop_pct` — `BARRIER_STOP_PCT` (1.0), `cost` —
`RISK_COST_ROUNDTRIP_PCT` (0.22), **числом в коде не зашит** (проверяется разбором
литералов в разделе 9 `verify_8_8.sh` и тестом `test_cost_is_a_parameter_not_a_literal`).

Четыре решения, которые легко было принять неверно незаметно, и как они приняты:

1. **«Задета» — касание внутри свечи** (`high`/`low`), не закрытие. Сравнение
   **нестрогое**: касание ровно в границу засчитывается (два отдельных теста).
2. **Свеча решения в собственное окно не входит.** Окно строго `t+1 … t+h` баров
   выбранного разрешения — как в `src/risk/targets.py`. Момент `t` усекается вниз
   до сетки баров, бар решения исключается. Отдельный тест кладёт в ряд «громкий»
   бар ровно в момент сигнала и требует, чтобы он **не** повлиял.
3. **Одновременное касание не разрешается догадкой.** Проверка «задеты обе»
   выполняется **раньше** проверок «задета одна» — обратный порядок молча
   превращал бы неразрешимый случай в `target` или `stop` в зависимости от того,
   какую ветку написали первой.
4. **`mae_pct`/`mfe_pct` считаются по ВСЕМУ окну, а не до момента касания.** Они
   описывают **окно**, а не сделку. Обрезка сделала бы §8 неотвечаемым: при пределе
   0.3% сделка закрылась бы раньше, чем при 1.0%, и обрезанное значение отвечало бы
   на чужой вопрос.

Отдельно про `no_data`: неполный ряд **не выдаётся за `timeout`**. «До срока не
задета ни одна» — утверждение обо всём окне; сделать его по ряду с дырой значит
утверждать то, чего не наблюдали. При этом касание, случившееся **до** разрыва,
остаётся фактом (тест `test_gap_after_hit_keeps_the_hit`).

`net_pnl_pct` (§5): `target` → `+target_pct − cost`; `stop` → `−stop_pct − cost`;
`timeout` → фактическое движение к сроку со знаком по направлению `− cost`;
`ambiguous` и `no_data` → `NULL`. Ноль вместо `NULL` был бы утверждением «человек
не заработал и не потерял», которого никто не мерил.

---

## §6. Схема

**Отклонение от ТЗ, о котором нужно знать:** ТЗ называет файл
`014_barrier_outcomes.sql`, но номер **014 уже занят** миграцией Этапа 8.2
(`014_risk_targets.sql`, применена на сервере). Два разных файла под одним номером
сделали бы порядок применения неопределённым, поэтому номер сдвинут на следующий
свободный: **`015_barrier_outcomes.sql`** (откат — `015_barrier_outcomes_rollback.sql`).
Содержание таблицы соответствует ТЗ полностью, включая типы и первичный ключ.

Таблица `signal_outcomes_barrier` — состав ровно по §6. Сверх ТЗ добавлены только
**ограничения**, потому что база обязана ловить ошибку расчёта раньше отчёта:

* `outcome` — `CHECK` ровно на пять значений §3;
* `resolution` — `CHECK IN ('1m','1h')`;
* `direction` — `CHECK IN ('buy','sell')`; `logic_version > 0`; `horizon_h > 0`,
  `price_at_signal > 0`, `stop_pct > 0`;
* **согласованность формы** (`..._shape_chk`): у `target`/`stop` обязаны быть
  `hit_at`, `bars_to_hit` и `net_pnl_pct`; у `timeout` — только `net_pnl_pct`;
  у `ambiguous` и `no_data` — ни того, ни другого, ни третьего.

Таблица внесена в `PROTECTED_TABLES` (`scripts/retention.py`) — не удаляется никогда.

**Проверено [стенд]:** применение → повторное применение → откат → повторное
применение, без ошибок; 8 ограничений на месте (6 `CHECK` + первичный и внешний ключ).
Та же последовательность выполняется тестом `test_migration_is_idempotent_and_reversible`
на живой базе.

> Тест **разрушительный** (откат удаляет таблицу). Поэтому в него встроен отказ:
> если в `signal_outcomes_barrier` есть строки, он **не выполняется**, а
> пропускается с объяснением. `AT_TEST_DSN` обязан указывать на одноразовую базу.
> Эта страховка появилась не из осторожности, а по факту: во время работы тест
> снёс засеянные данные стенда.

---

## §7. Где считается

* Точка входа `src/barrier_main.py`, сервис `barrier` в профиле **tools**
  (`docker-compose.yml`) — постоянный контейнер не создаётся.
* Расписание **04:10 UTC** — `deploy/agent-trade-barrier.cron` и строка в
  `deploy/install.sh`. Порядок содержательный: 03:40 цели и политика хранения →
  **04:10 расчёт** → 06:20 выгрузка. Расчёт идёт **после** политики хранения
  намеренно: она удаляет минутные свечи старше `RETENTION_1M_DAYS`, и запуск до неё
  дал бы разрешение `1m` по свечам, которых через полчаса уже не будет, — то есть
  результат, невоспроизводимый повторным прогоном.
* **Горячего пути нет:** `decision`, `notify`, `evaluator` не изменялись.
* Считаются только сигналы, у которых `t + h` уже в прошлом (условие в SQL-запросе
  отбора, а не в коде поверх него). Раздел 6 `verify_8_8.sh` отдельно проверяет,
  что исходов у ненаступивших горизонтов нет.
* **Обратный досчёт** по всем сигналам v5 с замороженными целями выполняется
  штатным запуском без флагов.
* **Сигналы без замороженных целей пропускаются**, их число печатается отдельным
  ключом `skipped_no_target` и отдельной строкой в `verify_8_8.sh`. Подстановка
  сегодняшних целей из `risk_targets` невозможна конструктивно: запрос отбора
  соединяется с `signal_targets` и требует `target_pct IS NOT NULL`. Раздел 8
  `verify_8_8.sh` дополнительно сверяет каждую строку исхода с замороженной строкой
  и объявляет расхождение блокирующим («история подменена»).

### Идемпотентность — доказана снимком, а не обещанием [стенд]

```
ПРОГОН 1: written=6   snapshot sha256 = 93396d097798ada692a9dcdcb271018d8fd6cc263ca93ff586bf463a9395ecc1
ПРОГОН 2: written=0   snapshot sha256 = 93396d097798ada692a9dcdcb271018d8fd6cc263ca93ff586bf463a9395ecc1
```

Повторный запуск на тех же данных не изменил **ни одной строки**; снимок таблицы
совпал побайтно. Механизм — `ON CONFLICT DO NOTHING` по первичному ключу плюс отбор
только непосчитанных пар.

`--recompute` требуется **явно** и сначала удаляет строки. Причина не в осторожности:
пересчёт старого сигнала может **понизить** разрешение, потому что минутные свечи к
тому времени удалены политикой хранения. То, что однажды измерено по минутам, во
второй раз посчиталось бы по часам — и молча стало бы хуже.

---

## Сквозной прогон конвейера [стенд]

Данные подобраны так, чтобы в одном прогоне встретились **все пять исходов** и
**оба разрешения**, и ответ по каждому был известен заранее.

```
barrier_horizon_done=1  candidates=6 written=6 skipped_no_target=1
  outcome_target=2 outcome_stop=1 outcome_timeout=1 outcome_ambiguous=1 outcome_no_data=1
  resolution_1m=3 resolution_1h=3
barrier_compute_done=1  ambiguous_pct=16.67
barrier_metric_unusable=1  ambiguous_pct=16.67 threshold_pct=15.0
  detail=доля ambiguous выше порога — метрика в таком виде малопригодна
barrier_exit=0  written=6 stop_pct=1.0 cost_pct=0.22
```

Содержимое таблицы:

| signal | symbol | dir | outcome | res | bars_to_hit | net_pnl_pct | mae | mfe |
|---|---|---|---|---|---|---|---|---|
| 1 | BTC/USDT | buy | `target` | 1m | 90 | **0.780000** | 0.30 | 1.40 |
| 2 | ETH/USDT | sell | `target` | 1m | 90 | **0.780000** | 0.30 | 1.40 |
| 3 | SOL/USDT | buy | `stop` | 1m | 45 | **−1.220000** | 1.20 | 0.30 |
| 4 | XRP/USDT | buy | `ambiguous` | 1h | · | **·** | 1.50 | 1.50 |
| 5 | DOGE/USDT | buy | `timeout` | 1h | · | **−0.220000** | 0.40 | 0.40 |
| 6 | LTC/USDT | buy | `no_data` | 1h | · | **·** | 0.00 | 0.00 |

Каждое число сходится с §5 вручную: `1.0 − 0.22 = 0.78`; `−1.0 − 0.22 = −1.22`;
движение к сроку 0.0 → `0.0 − 0.22 = −0.22`; у `ambiguous` и `no_data` итога нет.
`mae`/`mfe` у `no_data` — нули-**заполнители** колонок `NOT NULL`, а не измерение;
отличить их от настоящего нуля позволяет сам исход.

---

## §4. Доля ambiguous

**[стенд] 16.67%** (1 из 6) — **выше порога 15%**, поэтому напечатана отдельная
строка, как требует ТЗ:

> **МЕТРИКА В ТАКОМ ВИДЕ МАЛОПРИГОДНА: ambiguous 16.67% > 15%**

Это число **не характеризует продакшн**: оно получено на искусственной выборке,
где случай `ambiguous` заведён намеренно, чтобы проверить срабатывание порога.
Доля на сервере станет известна после первого запуска; её печатает и журнал
(`barrier_compute_done=1 ambiguous_pct=…`), и раздел 7 `verify_8_8.sh`.

Доля строк, посчитанных по минутам **[стенд]: 50%** (3 из 6) — по построению выборки.

---

## §8. Замер противоположного отклонения

Запрос: `analysis/sql/07_stop_levels.sql`. Разделы: общая таблица, по горизонту,
по токену, токен × горизонт.

**Таблица [стенд] — ДЕМОНСТРАЦИЯ РАБОТЫ ЗАПРОСА, НЕ ИЗМЕРЕНИЕ РЫНКА.**
Пять строк искусственных данных. Использовать эти числа для суждения о пределе
нельзя ни в каком виде.

| stop_level | rows | would_stop | would_stop_pct | wasted | wasted_share_of_stopped_pct |
|---|---|---|---|---|---|
| 0.30 | 5 | 5 | 100.00 | 3 | 60.00 |
| 0.50 | 5 | 2 | 40.00 | 1 | 50.00 |
| 0.75 | 5 | 2 | 40.00 | 1 | 50.00 |
| 1.00 | 5 | 2 | 40.00 | 1 | 50.00 |
| 1.50 | 5 | 1 | 20.00 | 1 | 100.00 |
| 2.00 | 5 | 0 | 0.00 | 0 | · |
| 3.00 | 5 | 0 | 0.00 | 0 | · |

**Граница точности, которую обязан знать читатель.** Расчёт идёт по накопленным
`mae_pct` и `mfe_pct` — крайним отклонениям за всё окно. Порядок между ними в этих
двух числах не сохранён, поэтому `wasted` отвечает на вопрос «дошла ли цена до цели
**в том же окне**», а не «дошла ли **после** того, как задела предел». Для выбора
уровня предела эта оценка **сверху** достаточна: она завышает долю напрасных
срабатываний, а не занижает. Точный порядок восстанавливается только повторным
проходом по свечам.

**Порог не изменён.** `BARRIER_STOP_PCT` остаётся 1.0 — ТЗ прямо требует доложить
таблицу и остановиться. Реальную таблицу владелец получит, выполнив запрос на
сервере (шаг 6 runbook).

---

## §9. Сравнение двух метрик

Запрос: `analysis/sql/08_metric_compare.sql`. Разделы: покрытие (у скольких пар есть
обе оценки), совпадение вердиктов по горизонтам, итог по всем горизонтам, полная
таблица соответствия «старый вердикт × новый исход», разрешение, и расхождения
поимённо (первые 50) для разбора руками.

Вердикты приравниваются так: старый успех ↔ новый `target`; `ambiguous` и `no_data`
вердикта **не имеют** и в согласие не считаются — иначе неизвестность засчитывалась
бы как совпадение.

**[стенд] — снова демонстрация запроса, не измерение:**

| horizon_h | pairs | no_verdict | agree | agree_pct | old_fail_new_target | old_success_new_stop | avg_old_pnl | avg_new_net_pnl |
|---|---|---|---|---|---|---|---|---|
| 4 | 6 | 2 | 2 | 50.00 | 2 | 0 | 0.0000 | 0.0300 |

Два случая `old_fail_new_target` — ровно та ситуация, ради которой этап и делался:
цена доходила до цели внутри окна и вернулась; замер к сроку прибыли не увидел,
а человек с ордером на цель её забрал бы.

**Выводов о том, какая метрика лучше, не делается** — ТЗ этого прямо не позволяет,
и данных за первые сутки на такой вывод не хватит. Задача раздела — подготовить
сравнение, и оно подготовлено.

---

## §10. Критерии приёмки — построчно

| # | Критерий | Статус | Где получено |
|---|---|---|---|
| 1 | §2 выполнена, числа приведены | ⚠️ **частично** | скрипт написан и исполнен **[стенд]**; чисел **[сервер]** нет — доступа не было. Гейт закрывается шагом 1 runbook |
| 2 | Отпечаток `1f12d5d2…de18d` | ✅ | **[стенд]**, 323 случая |
| 2b | Слепок снимается в контейнере; дефект `verify_8_7.sh` исправлен | ✅ | исправлено в **обоих** скриптах |
| 3 | Миграция применяется, идемпотентна, откатывается, применяется вновь | ✅ | **[стенд]** + тест на живой базе |
| 4 | PK не даёт задвоить; повторный запуск даёт побайтно тот же снимок | ✅ | **[стенд]**, sha256 совпал; отдельный тест на `UniqueViolationError` |
| 5 | Тесты на синтетических рядах: цель раньше предела, предел раньше цели, обе в одном баре, ни одна, разрыв ряда; касание ровно в границу | ✅ | 36 тестов, все шесть случаев отдельными тестами |
| 6 | Доля ambiguous числом; свыше 15% — отдельная строка | ✅ | **[стенд]** 16.67% → строка напечатана |
| 7 | `deploy/verify_8_8.sh` с тремя классами находок и явной итоговой строкой | ✅ | 12 разделов; исполнен **[стенд]** |
| 8 | **Полный конвейер внутри собранного образа** | ❌ **НЕ ВЫПОЛНЕНО** | образ собрать невозможно: сеть до Docker Hub закрыта политикой (см. ниже) |
| 9 | Пересборка `--no-cache` и `--profile "*"` | ❌ **НЕ ВЫПОЛНЕНО** | та же причина |
| 10 | pytest и ruff зелёные | ✅ | **[стенд]**: 471 passed, 54 skipped, 0 failed; ruff — All checks passed. Из них 36 — тесты этапа (`tests/test_stage_8_8.py`), включая три на живой базе. До этапа было 433 passed |
| 11 | Рядом с каждым числом указана среда | ✅ | сквозная пометка **[стенд]**/**[сервер]** |

### Почему пункты 8 и 9 не выполнены — фактом, а не пересказом

```
$ docker compose --profile "*" build --no-cache
target decision: failed to solve: python:3.12-slim: failed to resolve source metadata
for docker.io/library/python:3.12-slim: unexpected status from HEAD request to
https://registry-1.docker.io/v2/library/python/manifests/3.12-slim: 429 Too Many Requests

$ docker compose up -d postgres
Image postgres:16-alpine Pulling
failed to copy: ... https://production.cloudfront.docker.com/... : Forbidden

$ curl -sS "$HTTPS_PROXY/__agentproxy/status"
"recentRelayFailures": [{ "kind": "connect_rejected",
  "detail": "gateway answered 403 to CONNECT (policy denial or upstream failure)",
  "host": "production.cloudfront.docker.com:443" }]
```

Базовые образы не скачиваются: слои блокирует политика egress (403), манифесты
отдают 429. Обходить запрет нельзя и не нужно — это ограничение среды исполнителя,
а не дефект этапа.

**Что сделано вместо, и чем это НЕ является.** Конвейер прогнан **штатной точкой
входа** `python -m src.barrier_main` против **настоящего PostgreSQL 16.13** с
применённой схемой и миграциями, на том же коде, что уйдёт в образ. Это **не**
доказательство работы в образе: образ добавляет свой слой зависимостей и свой
`PYTHONPATH`. Прогон в образе обязан выполнить владелец шагом 4 runbook, и
**фактический вывод приложить к этому отчёту** — до этого пункты 8 и 9 остаются
незакрытыми.

---

## Что НЕ сделано и почему

1. **§2 на сервере.** Нет доступа к продакшн-базе. Скрипт готов, шаг 1 runbook.
2. **Прогон в собранном образе (§10.8, §10.9).** Сеть до Docker Hub закрыта.
   Шаги 3–4 runbook.
3. **Слияние в `main`.** `origin/main` отстаёт на 38 коммитов и не содержит
   Этапов 8.1–8.7; слияние принесло бы туда всю эту историю разом. Решение за
   владельцем — команда в runbook, шаг 9.
4. **Порог `BARRIER_STOP_PCT` не изменён** — так требует §8 ТЗ.

---

## Runbook для сервера

Каждый шаг — одна команда. Выполнять по порядку под пользователем `agent` из
`/opt/agent-trade`.

**1. Предпроверка §2 (блокирующая; вывод приложить к отчёту).**
```bash
docker compose exec -T postgres psql -U agenttrade -d agenttrade -f /dev/stdin < scripts/precheck_8_8.sql
```

**2. Обновить код и применить миграцию.**
```bash
git fetch origin && git checkout claude/stage-8-8-barrier-outcomes-dehs65 && git pull --ff-only
```
```bash
docker compose exec -T postgres psql -U agenttrade -d agenttrade < db/migrations/015_barrier_outcomes.sql
```

**3. Пересобрать образы (обязательно `--no-cache`).**
```bash
docker compose --profile "*" build --no-cache && docker compose up -d --remove-orphans
```
> Порядок флагов: на Compose v5 `--profile` — **флаг верхнего уровня**;
> `docker compose build --profile "*"` отвечает `unknown flag: --profile`
> (проверено **[стенд]**, Compose v5.1.1). На v2 работали обе формы.

**4. Первый расчёт — обратный досчёт по всем сигналам v5 (вывод приложить).**
```bash
docker compose --profile tools run --rm --no-deps barrier
```

**5. Проверка развёртывания.**
```bash
sudo -u agent bash /opt/agent-trade/deploy/verify_8_8.sh
```

**6. Таблица §8 — уровни предела (вывод приложить, порог НЕ менять).**
```bash
docker compose exec -T postgres psql -U agenttrade -d agenttrade -f /dev/stdin < analysis/sql/07_stop_levels.sql
```

**7. Сравнение двух метрик §9 (вывод приложить, выводов не делать).**
```bash
docker compose exec -T postgres psql -U agenttrade -d agenttrade -f /dev/stdin < analysis/sql/08_metric_compare.sql
```

**8. Поставить регламент 04:10 UTC.**
```bash
sudo cp deploy/agent-trade-barrier.cron /etc/cron.d/agent-trade-barrier && sudo chmod 644 /etc/cron.d/agent-trade-barrier
```

**9. Слияние в main — только по решению владельца (см. «Что НЕ сделано», п. 3).**
```bash
git checkout main && git merge --no-ff claude/stage-8-8-barrier-outcomes-dehs65 && git push origin main && git push origin --delete claude/stage-8-8-barrier-outcomes-dehs65
```

### Откат этапа (действующая оценка не затрагивается)
```bash
docker compose exec -T postgres pg_dump -U agenttrade -d agenttrade -t signal_outcomes_barrier > /tmp/barrier_backup.sql
```
```bash
docker compose exec -T postgres psql -U agenttrade -d agenttrade < db/migrations/015_barrier_outcomes_rollback.sql
```
```bash
sudo rm -f /etc/cron.d/agent-trade-barrier
```

---

## Состав изменений

**Новое:**
`db/migrations/015_barrier_outcomes.sql` (+ rollback) · `src/barrier/__init__.py` ·
`src/barrier/outcomes.py` · `src/barrier/runner.py` · `src/barrier_main.py` ·
`scripts/precheck_8_8.sql` · `analysis/sql/07_stop_levels.sql` ·
`analysis/sql/08_metric_compare.sql` · `deploy/verify_8_8.sh` ·
`deploy/agent-trade-barrier.cron` · `tests/test_stage_8_8.py` · этот отчёт.

**Изменено:**
`src/core/db.py` (DDL и шесть методов доступа — существующие не тронуты) ·
`src/core/config.py` (четыре ключа `BARRIER_*`) · `.env.example` ·
`docker-compose.yml` (сервис `barrier`, профиль tools) ·
`deploy/install.sh` (строка cron) · `scripts/retention.py` (`PROTECTED_TABLES`) ·
`pyproject.toml` (пакет `src.barrier`) · `README.md` ·
`deploy/verify_8_7.sh` (**исправлен дефект раздела 8**: слепок снимался системным
python3 на хосте — то есть отвечал за код репозитория, а не за код образа, и на
исправной системе молча падал в ⚪, потому что pydantic/structlog на хост не
ставятся по правилу D-3).

**Не изменено ни одним символом:** `src/agents/`, `src/decision/`, `src/notify/`,
`src/evaluator/`, `src/risk/`, `db/init.sql`.
