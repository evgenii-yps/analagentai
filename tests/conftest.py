"""Общие настройки для тестов.

Пароль БД обязателен для инстанцирования Settings — задаём дефолт
до импорта любого модуля приложения при сборе тестов.
"""

import os

os.environ.setdefault("POSTGRES_PASSWORD", "test_password")

# =============================================================================
# ДЕЙСТВУЮЩАЯ ВЕРСИЯ ЛОГИКИ — В ОДНОМ МЕСТЕ НА ВЕСЬ НАБОР ТЕСТОВ
# =============================================================================
#
# ЗАЧЕМ ЭТА КОНСТАНТА ПОЯВИЛАСЬ (Этап 9.2 §1.1). Замерные этапы 8.7, 8.8, 8.9,
# 8.10, 9.1.5 и 9.1.6 закрепили тестами обещание «этот этап НЕ поднимает
# LOGIC_VERSION» — и закрепили его числом 5, вписанным в шесть файлов. Обещание
# верное и снимать его нельзя: замер, тихо изменивший правило системы,
# перестаёт быть замером. Но написанное шестью копиями числа, оно превращает
# ЛЮБОЙ законный подъём версии в правку шести тестов, и при седьмой копии
# кто-нибудь однажды поправит пять из шести.
#
# Этап 9.2 поднял версию 5 → 6 РЕШЕНИЕМ ВЛАДЕЛЬЦА: правило выхода позиции
# изменилось, и сделки версий 5 и 6 несравнимы. Число переехало сюда, а прежние
# проверки продолжают делать ровно то, ради чего написаны: они ловят версию,
# изменённую БЕЗ ведома того, кто правит эту строку.
#
# ТЗ «Версия логики 7» поднял версию 6 → 7 РЕШЕНИЕМ ВЛАДЕЛЬЦА: изменилось
# правило ДОПУСКА к сделке (слотов нет, правила «один инструмент — одна
# позиция» нет, есть пауза по токену), и сделки версий 6 и 7 несравнимы по той
# же причине, по какой несравнимы 5 и 6 — поток, на котором они посчитаны,
# отобран разными правилами.
CURRENT_LOGIC_VERSION = 7
