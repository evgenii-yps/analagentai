# Закреплённые версии для воспроизводимой сборки (Docker / CI).
asyncpg==0.30.0
redis==5.2.1
pydantic-settings==2.7.1
structlog==24.4.0

# Зависимости для тестов и линтинга.
pytest==8.3.4
pytest-asyncio==0.24.0
ruff==0.8.4
